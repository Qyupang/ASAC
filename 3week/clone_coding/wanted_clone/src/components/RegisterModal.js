import React from 'react';

const RegisterModal = ({ setModalOpen }) => {
  const closeModal = () => {
    setModalOpen(false);
  };
  return (
    <div>
      <span></span>
    </div>
  );
};

export default RegisterModal;
