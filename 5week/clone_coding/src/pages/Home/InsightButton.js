import React from 'react';

const InsightButton = ({ content }) => {
  return (
    <div className="career-insight__button">
      <span>{content}</span>
    </div>
  );
};

export default InsightButton;
